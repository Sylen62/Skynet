import React from 'react';
import { DarkTheme } from 'react-native-paper';

const theme = {
  ...DarkTheme,
  background: '#1A1A1A',
  foreground: '#FAFAFA',
  roundness: 30,
};

export default theme;
